apiVersion: v1
kind: Pod
metadata:
  name: pod-1
spec:
  containers:
    - name: nginx
      image: public.ecr.aws/v2b5j9n5/nginx_practise
      ports:
        - containerPort: 80
          hostPort: 80
          


# ---
# kind: Service 
# apiVersion: v1 
# metadata:
#   name: pod-service
# spec:
#   selector:
#     app.kubernetes.io/name: proxy
#   ports:
#     - protocol: TCP
#       port: 80
#       targetPort: 80


# apiVersion: v1
# kind: Pod
# metadata:
#   name: nginx
#   labels:
#     app.kubernetes.io/name: proxy
# spec:
#   containers:
#   - name: nginx
#     image: nginx:stable
#     ports:
#       - containerPort: 80
#         name: http-web-svc

# ---
# apiVersion: v1
# kind: Service
# metadata:
#   name: nginx-service
# spec:
#   selector:
#     app.kubernetes.io/name: proxy
#   ports:
#   - name: name-of-service-port
#     protocol: TCP
#     port: 80
#     targetPort: http-web-svc